import { View, Text } from "react-native";
import React from "react";
import { SafeAreaView } from "react-native-safe-area-context";

const Home = () => {
  return (
    <SafeAreaView>
      <View>
        <Text className="text-9xl">Home</Text>
      </View>
    </SafeAreaView>
  );
};

export default Home;
